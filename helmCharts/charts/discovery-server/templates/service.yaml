# /charts/spring-petclinic-service/template/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "discovery-server.fullname" .) }}
  labels:
    app: {{ include "discovery-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "discovery-server.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

