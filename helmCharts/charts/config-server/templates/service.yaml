# /charts/spring-petclinic-service/template/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "config-server.fullname" .) }}
  labels:
    app: {{ include "config-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "config-server.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

