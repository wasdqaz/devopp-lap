# /charts/spring-petclinic-service/template/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "genai-service.fullname" .) }}
  labels:
    app: {{ include "genai-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "genai-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

