# /charts/spring-petclinic-service/template/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "admin-server.fullname" .) }}
  labels:
    app: {{ include "admin-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "admin-server.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

